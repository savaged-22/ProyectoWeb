export const environment = {
  production: true,
  // En producción, si el frontend y backend están en el mismo dominio o usando un proxy inverso,
  // se puede usar una ruta relativa o la URL real de tu API.
  apiUrl: '/api'
};
