import { Component } from '@angular/core';

@Component({
  standalone: true,
  template: `
    <h2>Pool Settings (HU-21, HU-24)</h2>
    <p>Configuración de pool + matriz de permisos (placeholder).</p>
  `
})
export class PoolSettingsPage {}
